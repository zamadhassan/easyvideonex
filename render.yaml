services:
  - type: web
    name: easyvideonex-api
    runtime: python
    plan: free
    rootDir: backend-api
    buildCommand: pip install -r requirements.txt
    startCommand: python python-server.py
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
